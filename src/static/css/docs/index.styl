@import 'vars'

sidebarWidth = unit * 32
contentWidth = 78 * unit

.docs
  background transparent
  @import 'sidebar'
  @import 'loader'

  .content
    @media (max-width: 1200px)
      padding-left sidebarWidth

    @import 'api'
    @import 'markdown'
    @import 'pagination'

