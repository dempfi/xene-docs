@import 'vars'

sidebarWidth = 28 * unit

.docs
  background transparent
  align-items flex-start
  max-width pageWidth
  position relative
  margin 0 auto
  display flex

  @import 'sidebar'
  @import 'loader'

  .content
    margin-right unit * 8
    flex-grow 1
    @import 'reference'
    @import 'markdown'
    @import 'pagination'

