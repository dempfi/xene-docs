@import 'vars'

sidebarWidth = unit * 32
contendWidth = 80 * unit

.docs
  background transparent
  @import 'sidebar'
  @import 'loader'

  .content
    @media (max-width: 1200px)
      padding-left sidebarWidth

    @import 'markdown'
    @import 'pagination'

